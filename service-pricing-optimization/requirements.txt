# Pricing API service dependencies.
#
# This service reuses src/optimization (see app/services/pricing_service.py)
# rather than reimplementing it, so it needs that package's own runtime
# dependencies too, not just a web framework. Keep this in sync with the
# monorepo's root requirements.txt when src/'s dependencies change.

# Web framework
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.29.0,<1.0
pydantic-settings>=2.2.0,<3.0

# Required by src/ (traced from src.config, src.data, src.optimization,
# and src.models.elasticity -- the exact import chain this service uses).
# src/ has other, heavier dependencies (scikit-learn, xgboost, scipy,
# linearmodels...) for pipeline steps this service never imports; they're
# deliberately left out to keep this service's own image lean.
numpy>=1.24.0,<3.0
pandas>=2.2.0,<4.0
statsmodels>=0.14.0,<1.0
joblib>=1.3.0,<2.0

# Tests
pytest>=7.4.0
httpx>=0.27.0,<1.0
